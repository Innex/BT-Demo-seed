You are generating domain-specific data for a Braintrust demo seed script.

Company: {company_name}
AI Product: {ai_product_name}
Description: {ai_product_description}
Domain: {product_domain}
Input type: {input_type}
Output type: {output_type}
Feature modes: {feature_modes}
Evaluation dimensions: {evaluation_dimensions}

Generate:
1. A **system prompt base** — the core instructions for the AI product
2. A **style A suffix** — precise, structured, technical response style
3. A **style B suffix** — conversational, insight-driven, friendly style
4. Style names for each (e.g., "structured" and "conversational", or "concise" and "detailed")

EXAMPLE shape (de-branded — replace product name, capabilities, and domain wording with content specific to {ai_product_name}):

system_prompt_base: "You are <product name>, <one-sentence role>. You <core action> based on <input>. You have access to the user's schema: <entity-type names> and <property-type names>.\n\nRules:\n- Only reference entities and properties that exist in the project schema\n- Select the correct feature mode for the request\n- If the request is ambiguous, pick the most likely interpretation and state your assumption\n\nProject schema:\nEntities: {{{{metadata.schema_entities}}}}\nProperties: {{{{metadata.schema_properties}}}}"

style_a_suffix: "\n\nStyle: Be precise and output-focused. Lead with the result. Keep it concise."

style_b_suffix: "\n\nStyle: Be explanatory and insight-driven. Highlight patterns. Ask follow-up questions."

IMPORTANT: The seed template logs traces with these metadata keys. The system prompt may reference ONLY these:
- {{{{metadata.schema_entities}}}} — comma-separated domain entity names
- {{{{metadata.schema_properties}}}} — comma-separated entity property names
- {{{{metadata.feature_mode}}}}, {{{{metadata.vertical}}}}, {{{{metadata.query_complexity}}}} — context fields

DO NOT invent metadata keys. If a key is not in the list above, do not reference it — the trace will not contain it.

Return JSON:
{{
  "system_prompt_base": "...",
  "style_a_name": "structured",
  "style_a_suffix": "...",
  "style_b_name": "conversational",
  "style_b_suffix": "..."
}}

Adapt to {ai_product_name}'s specific domain and capabilities. Return ONLY valid JSON.